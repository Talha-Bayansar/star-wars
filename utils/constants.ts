export const swUrl = "https://swapi.dev/api";

export enum API_URLS {
  films = `${swUrl}/films`,
  people = `${swUrl}/people`,
}

export enum APP_URLS {
  home = "/",
  movies = "/movies",
  characters = "/characters",
}
